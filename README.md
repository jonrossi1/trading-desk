# MVP Trading Desk

This project is designed to be shared safely.
Code and strategy configs are shared.
IBKR accounts and settings are local to each person.

## Setup
- Create a Python virtual environment
- Install dependencies from requirements.txt
- Create a local .env file from .env.example

## Running safely
Run in paper mode and dry-run by default.

Logs are written to logs/desk.log.
